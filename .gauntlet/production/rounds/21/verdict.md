# Round 21 — отчёт и прогресс для оператора — lens: resume

WINNER: B
GAP: неуникальная метка прогона нашлась случайно — регрессия начала падать от порядка запуска, и только это привело к дефекту; целенаправленной проверки «различает ли метка два быстрых прогона» не было ни в раунде 11, где механизм объявлен закрытым, ни здесь до самого падения теста
WHY: линза требует замера «сколько работы пропало» и числами. Одна сторона проверяет, что
об этом ГОВОРИТ отчёт, и находит три отсутствующих ответа: полное время, состав прогонов,
был ли сбой вообще. Показывает цену: серия 314 шотов, упавшая на 200-м и досчитанная за
16 минут, отчитывалась как «16 минут» при фактических 46 — ошибка 2.9x в цифре, по
которой планируют следующую ночь. Лечение доведено до терминала (`this episode took 2
attempt(s): 50.6s of wall time in total`), а не оставлено в словаре. Плюс находит
дефект в механизме, объявленном закрытым два раунда назад: `run_id` из секунд и PID
давал одинаковую метку двум прогонам в одну секунду из одного процесса, то есть
обещание раунда 11 «метка различает прогоны» при быстром досчёте не выполнялось. Вторая
сторона формулирует оба требования верно, включая «метка обязана различать», но ни одной
проверки не делает.
Проигрывает первая сторона на способе, которым нашла второй дефект: не проверкой, а
падением теста от порядка запуска. Механизм, объявленный готовым в раунде 11, три раунда
жил с дефектом, который снимается одним вопросом — «а если два прогона в одну секунду?».
FIX: после каждого «механизм готов» выписывать условия, при которых он перестаёт
работать (одна секунда, один процесс, один PID, пустой ввод), и проверять их сразу —
дешевле одного теста, а не трёх раундов.

## Reveal

- A = candidate
- B = reference
- picked: B → **BAR WINS**
- You picked B, which was the reference.
