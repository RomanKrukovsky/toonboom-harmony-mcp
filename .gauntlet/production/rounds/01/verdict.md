# Round 01 — очередь эпизода — lens: throughput

WINNER: B
GAP: 12 воркеров дают лишь 1.8× ускорения вместо 8× — почти всё съедает запуск отдельного процесса Blender на каждый шот (6.02 с на 72 кадра, из них счёт кадров ~1.4 с)
WHY: B задаёт цель ≥2.7 кадра/с И ≥8× ускорения, то есть требует, чтобы параллелизм
был настоящим, а не номинальным. A показывает 11.96 кадра/с — формально выше цели по
абсолютной цифре, но 1.8× из 8× означает, что 12 занятых воркеров работают как 2:
на демо-шоте в 12 кадров запуск Blender дороже самого рендера. На серии из 314 шотов
это 44 минуты вместо 25 и, что важнее, масштаб не растёт с ядрами.
Отдельно: A печатает «накладные на шот ~-0.83 с» — отрицательное число, то есть моя
же формула замера бессмысленна и не может указать на причину.
FIX: считать несколько шотов в ОДНОМ процессе Blender (батч по воркеру) вместо
процесса на шот; заодно исправить формулу накладных, чтобы она не давала минус.

## Reveal

- A = candidate
- B = reference
- picked: B → **BAR WINS**
- You picked B, which was the reference.
