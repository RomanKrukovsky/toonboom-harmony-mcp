# Round 19 — отчёт и прогресс для оператора — lens: throughput

WINNER: B
GAP: сводка по причинам падений проверена только на синтетическом списке, собранном руками — через настоящий CLI она не прогонялась ни разу, потому что попытка сломать рисунки провалилась (`demo_episode` пересоздаёт их), и я не стал доводить пробу до конца
WHY: линза требует замера на объёме серии, а не оценки. Одна сторона мерит учёт на трёх
масштабах и на 314 шотах даёт число: 0.65 с из ~2700 с прогона = 0.024%. Находит
квадратичность (`Ledger` создаётся заново на каждую запись — 628 шотов 3.02 с против
0.102 с, 30x), измеряет её, экстраполирует до сезона (~165 с) и осознанно НЕ трогает,
записав в очередь с числами и готовым лечением — это ровно то, что линза называет
«смотреть на замер»: решение не оптимизировать здесь обосновано, а не унаследовано.
Дальше она замеряет вторую половину той же линзы, которую бар называет прямо
(«отчёт читает человек, его время тоже расходуется»): 1920 строк на серии, из них 98%
машинных дублей того, что уже лежит в реестре, — и после правки отчёт становится
постоянного размера, 33 строки на 12 шотах и на 314. Вторая сторона формулирует оба
требования, включая важное про время человека, но ни одного числа не даёт.
Проигрывает первая сторона на том, что одну из двух своих правок не проверила тем
путём, которым её увидит оператор. Синтетический список доказывает, что группировка
считает верно; он не доказывает, что строки вообще доходят до терминала — а раунд 12
показал, что это отдельный вопрос, и ответ на него бывает «нет».
FIX: прогнать массовое падение через настоящий CLI (renderer, валящий часть шотов,
через `--json` и без него) и убедиться, что сводка печатается и попадает в
`report.json`; добавить регрессию на ВЫВОД, а не только на функцию группировки.

## Reveal

- A = reference
- B = candidate
- picked: B → **OURS WINS**
- You picked B, which was the candidate.
- Self-recognition check: you built this candidate, so you may have recognised it rather than judged it. Re-read the rubric line you scored on — if your stated reason does not survive that re-read, treat this as BAR WINS and keep going.
