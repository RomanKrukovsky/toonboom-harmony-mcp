# Round 04 — очередь эпизода — lens: assembly

WINNER: A
GAP: мастер собирается перекодированием каждого шота в промежуточный MP4 (12 сегментов на 12 шотов), то есть на серии из 314 шотов это 314 лишних проходов libx264 по всем кадрам — время сборки растёт линейно там, где concat мог бы работать почти мгновенно
WHY: по формулировке линзы («один мастер со звуком, длительность равна сумме шотов
±0.1 с, порядок по раскадровке») наша сторона отвечает всеми числами разом: один
файл, оба потока, 72 кадра из 72, расхождение +0.023 с внутри допуска ±0.1 с,
порядок 12 шотов верный (проверен цветом, а не именами файлов), швы чистые — ни
чёрного кадра, ни пропущенного. Бар требует четыре свойства; мы проверяем шесть,
включая швы и точный счёт кадров, которых бар не называет.
Слабое место — цена, а не корректность: сборка идёт через промежуточный сегмент на
каждый шот, и это единственное место в конвейере, которое масштабируется хуже
линейного счёта кадров.
FIX: замерить время сборки на 314 сегментах и, если оно заметно, склеивать кадры
напрямую в мастер одним проходом, оставив промежуточные сегменты только для шотов
со своим звуком.

## Reveal

- A = reference
- B = candidate
- picked: A → **BAR WINS**
- You picked A, which was the reference.
