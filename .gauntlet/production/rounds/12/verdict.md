# Round 12 — возобновление после аварии — lens: operability

WINNER: A
GAP: прогресс не доставляется во время работы — 11 строк с растущей `eta` буферизуются и вываливаются пачкой в конце; в ночном логе, который предлагает сама документация, это 10.6 с тишины на 14 шотах и ~45 минут неизменного файла на серии 22 минуты
WHY: линза называет «видимый прогресс» отдельным требованием и велит смотреть на то,
что оператор видит в терминале. По этому пункту одна сторона предъявляет замер — время
появления строки против времени события — и показывает, что все 11 строк приходят на
7.3 с, хотя `frames_per_s` внутри них растёт 3.96 → 14.55, то есть события были разными
по времени. Дальше она проверяет тот самый путь, который предлагает документация
(`> night.log`), и получает `0.5s → 3 строки, 11.1s → 19 строк`: в файл Python
буферизует блоками, не строками, поэтому дефект бьёт именно по ночному прогону, а не
по интерактивному запуску. Она же ловит «1241336 fps» и «rendered 12 frames» при нуле
работы — утверждение о работе, которой не было. Вторая сторона формулирует требование
правильно и полнее по весу пунктов, но остаётся требованием: она не проверяет ни один
из них и не может сказать, выполнены они или нет. По линзе, которая прямо велит
смотреть на терминал, замер терминала весит больше, чем формулировка того, каким он
должен быть.
FIX: печатать прогресс с `flush=True` (и/или строчной буферизацией stdout), чтобы
строка выходила в момент события; не печатать «rendered N frames», когда счёта не было
— сказать прямо «серия уже готова, считать нечего»; не печатать скорость при нулевом
интервале.

## Reveal

- A = reference
- B = candidate
- picked: A → **BAR WINS**
- You picked A, which was the reference.
