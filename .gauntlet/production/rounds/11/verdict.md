# Round 11 — возобновление после аварии — lens: traceability

WINNER: B
GAP: `PRODUCTION.md` обещает раздел «Кто сделал этот кадр», но `episode.py` ни разу не вызывает `Ledger` — на серии, посчитанной штатной командой, `provenance.jsonl` не создаётся и обещанный документацией отчёт возвращает `untracked` на любой кадр
WHY: линза спрашивает прямо — «на случайный кадр серии отвечается, кто его сделал?» — и
велит смотреть отчёт по кадру, выбранному наугад, а не по удобному. Наша сторона взяла
кадр 3 настоящей серии, посчитанной `episode.py --demo`, и получила
`{"verdict": "untracked", "styles_used": [], "touches": []}`. То есть на вопрос линзы
ответ — «нет», и сторона сама это доказала замером, а не умолчала. Бар при этом
формулирует требование именно на уровне кадра («на любой кадр можно ответить, кто его
сделал»), и по существу выигрывает: механизм есть и проверен 44 тестами, но к конвейеру
не подключён, а документация утверждает обратное — то есть обещание студии не
выполняется. Вторая проба нашей стороны (принадлежность шота прогону и версии Blender)
честно помечена как добавление к требованию, а не его часть, и в разрыв раунда не идёт.
FIX: вызывать `Ledger` из `run_episode` — писать `provenance.jsonl` в каталог серии,
регистрируя на каждый посчитанный шот источник рисунков, отпечаток описания и то, что
сгенерировано; затем проверить командой из `PRODUCTION.md`, что кадр наугад отвечает не
`untracked`.

## Reveal

- A = candidate
- B = reference

## Reveal

- A = candidate
- B = reference
- picked: B → **BAR WINS**
- You picked B, which was the reference.
