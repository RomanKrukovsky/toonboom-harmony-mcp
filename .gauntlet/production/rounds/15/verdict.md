# Round 15 — сборка мастера — lens: resume

WINNER: B
GAP: `report.json` писался в `run_episode` — ДО склейки — и при успехе больше не перезаписывался: длины мастера, пропущенных шотов, порчи кадров и переиспользованных сегментов в нём не было вообще, хотя `PRODUCTION.md` велит утром смотреть именно его, а не закрытый терминал
WHY: линза требует замер «сколько работы пропало», а не наличие журнала. Одна сторона
предъявляет его на обеих половинах пути: рендер пропускал готовое, склейка — нет, и цена
названа числами (1137 мс на сегмент при 1080p, 357 с на серию 314 шотов против нужных
0.7 с = 5.9 минуты лишней работы на КАЖДЫЙ досчёт). Она же перечисляет четыре условия
переиспользования и обосновывает каждое конкретным способом ошибиться — обрубок от
прерванной сборки, сегмент от старых кадров при правке тайминга (те же имена файлов),
перезаписанный звук, смена разрешения (не меняет ни одного имени и по времени не
ловится) — и проверяет все четыре: пересобирают шот. Дальше находит, что проверка кадров
из прошлого раунда стоила 373 мс на шот (117 с на серию) из-за `-count_frames`, и
заменяет её заголовком, доказав на урезанном до трети файле, что оба интересующих случая
различимы без декодирования. Вторая сторона формулирует требование к замеру и к условиям
верно, но ни одного числа не даёт.
Проигрывает первая сторона на побочной находке, которую сама же и называет: отчёт на
диске при успехе не содержал результата сборки. Для линзы resume это прямой удар —
оператор, у которого ночь упала, открывает утром report.json и не видит, чем кончилась
склейка.
FIX: перезаписывать `report.json` после сборки (сделано в этом же раунде) и добавить
регрессию, что при успешном прогоне ключ `assembly` в файле есть — иначе следующая
правка порядка записи снова его потеряет.

## Reveal

- A = reference
- B = candidate

## Reveal

- A = reference
- B = candidate
- picked: B → **OURS WINS**
- You picked B, which was the candidate.
- Self-recognition check: you built this candidate, so you may have recognised it rather than judged it. Re-read the rubric line you scored on — if your stated reason does not survive that re-read, treat this as BAR WINS and keep going.
