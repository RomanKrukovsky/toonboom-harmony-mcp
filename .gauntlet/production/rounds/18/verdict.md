# Round 18 — сборка мастера — lens: operability

WINNER: B
GAP: волновая формула прогноза проверена только на одном масштабе (30 шотов, 12 воркеров) — на серии 314 шотов, где ошибка старой формулы была 1.0×, новая не измерена вовсе, то есть замена сделана без доказательства, что она не портит тот случай, ради которого конвейер существует
WHY: линза требует смотреть на то, что видит оператор, и бар говорит «запускает человек,
который не я». Одна сторона проверяет ровно это: берёт фразу из документации
(«assemble again», трижды) и показывает, что выполнить её нельзя — ключа нет, а полный
прогон ради одной склейки повторяет preflight и учёт 314 шотов; при потерянном описании
серии команда вообще отказывается работать при живом каталоге кадров. Добавляет
`--assemble-only` (0.5 с) и подсказку «как досчитать», которая нужнее именно здесь.
Дальше находит, что первое число прогресса врало в 11 раз (eta 301 с при фактических
26 с), объясняет причину — скорость измерена по одному завершённому шоту, когда работали
двенадцать, — и заменяет формулу на волновую (ошибка 1.1×). Отдельно ценно, что она
сняла ложную тревогу замером: 39 с тишины и пачка `ok` выглядели как дефект раунда 12, но
журнал показал шоты по 34–38 с при 12 воркерах на 10 шотов, то есть вывод был верен.
Вторая сторона формулирует критерий точно, включая «ошибочный прогноз хуже
отсутствующего», но ни одной команды не запускает.
Проигрывает первая сторона на неполноте своего же замера: она сама приводит таблицу, где
на 314 шотах старая формула давала 1.0×, и меняет её, не измерив новую на том масштабе.
Для линзы, где решает поведение у оператора, это оставленный без ответа вопрос — не
станет ли на реальной серии хуже.
FIX: смоделировать волновую формулу на 100 и 314 шотах против фактического времени и
записать обе ошибки рядом; если на большом масштабе она проигрывает — переключаться по
числу завершённых волн, а не заменять формулу целиком.

## Reveal

- A = candidate
- B = reference
- picked: B → **BAR WINS**
- You picked B, which was the reference.
