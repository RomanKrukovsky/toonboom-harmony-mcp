# Prompt to Scene Demo

This example shows how a prompt is decomposed into a Harmony scene plan (`scene_plan.json`).

## Example

Input: "Wide shot of dark laboratory. Professor enters frame."
Output: A valid `scene_plan.json` with camera, background, and character action defined.
