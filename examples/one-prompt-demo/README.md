# One Prompt Demo

This example demonstrates how the One-Prompt Engine converts a text prompt into a full episode package.

## Running the demo

Use the `harmony.oneprompt.run_to_preview_episode` tool with a demo prompt:
`"Create a 2-minute 2D cartoon about a mad professor and a nervous student in a sci-fi lab."`
