# База Знаний: Курс по Риггингу в Toon Boom Harmony и Интеграция с MCP-Сервером (`toonboom-harmony-mcp`)

Данный документ представляет собой полное исследование обучающего плейлиста Гликерии Узинской по риггингу персонажа в Toon Boom Harmony, структурированный анализ всех 13 видеоуроков, паттернов работы и их прямую интеграцию с инструментами MCP-сервера `toonboom-harmony-mcp`.

---

# ЧАСТЬ 1: Детальный пошаговый анализ каждого видео плейлиста

---

## Видео 1: Риг персонажа. Персонаж в Toon Boom Harmony / начало # 1 // Простой риггинг

### 1. Главная тема видео
Первоначальная настройка Toon Boom Harmony, конфигурация глобальных предпочтений (Preferences), организация рабочего пространства (Workspace), импорт референсного персонажа (PNG/векторная конвертация) и создание цветовой палитры.

### 2. Что автор пытается донести
Правильный старт сцены предопределяет чистоту рига. Выключение автоматического создания элемент-нод при анимации предотвращает появление ключей на арт-слоях. Разделение осей позиционирования (`separate positional axes`) гарантирует независимое управление координатами X/Y/Z. Перевод PNG в вектор сохраняет сочность цветов и исключает необходимость ручной подгонки оттенков.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 00:40 | Введение, план курса | Объяснение структуры уроков | Понимание целей | Вспомогательно |
| 2 | 00:41 - 01:10 | Настройка Preferences -> General -> Default Separate Position | Разделение осей X, Y, Z | Независимые контроллеры | Критично |
| 3 | 01:11 - 01:30 | Включение Focus Mouse, Support Overlay & Underlay | Подготовка 4 суб-слоев рисунка | Слои L, C, O, U | Критично |
| 4 | 01:31 - 01:50 | Выключение "Element Node Creation" | Избежание ключей на Drawing-нодах | Чистая анимация на Pegs | Критично |
| 5 | 01:51 - 02:40 | Настройка и сохранение Workspace (Rigging) | Оптимизация расположения панелей | Сохраненный Workspace | Желательно |
| 6 | 02:41 - 03:40 | Импорт референса (PNG) с векторной конвертацией | Получение чистых численных цветов | Цветной векторный арт | Критично |
| 7 | 03:41 - 04:30 | Создание палитры и пипетка по растру | Фиксация оригинальных цветов персонажа | Полная палитра | Критично |

### 4. Визуальные действия в интерфейсе
* **Preferences**: General (Separate Position), Advanced (Support Overlay/Underlay, Disable Element Node Creation).
* **Workspace**: Перетаскивание панелей Node View, Library, Tool Properties. Сохранение через `Windows -> Workspace -> Save As`.
* **File Import**: `File -> Import -> Images -> Vectorize`.
* **Color View**: Создание образцов цвета (`+`), взятие образца пипеткой.

### 5. Повторяемые паттерны
* Выставление точных глобальных настроек Harmony.
* Конвертация растра в вектор для сохранения профилей цвета.
* Формирование персонажной палитры с именованием каждого образца.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Проверка/Настройка предпочтений | Да | `harmony.validate_environment` / `harmony.get_config` | Настройки среды | Текущая конфигурация Harmony | Высокая |
| Импорт референса | Да | `harmony.drawings.import_image` | `path`, `vectorize=true`, `color_mode="color"` | ID созданного Drawing node | Высокая |
| Создание цвета в палитре | Да | `harmony.palette.create_colour` | `palette_name`, `color_name`, `rgba` | ID цвета в палитре | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Добавить в `harmony.validate_environment` автоматическую проверку опций `separate_position_axes=true` и `element_node_creation=false`.

### 8. Итог по видео
* Видео учит фундаментальной подготовке сцены.
* Ключевые действия: раздельность осей, 4 суб-слоя (L/C/O/U), отключение Element Node, подготовка палитры.
* Ручная работа: визуальная выверка цветов при импорте.

---

## Видео 2: Риг в ToonBoom Harmony / Node система # 2

### 1. Главная тема видео
Построение базовой нодовой структуры (Node View) персонажа, выстраивание иерархии композитов (Head, Torso, Body Down, Master Composite) и первичное прикрепление пегов (`Cmd+Shift+P`).

### 2. Что автор пытается донести
Нодовая система — сердце Cut-out рига. Вместо плоского списка слоев персонаж делится на анатомические узлы через Композиты (`Composite`). Важно использовать правильный режим Composite (Pass Through / Bitmap), чтобы маски и прозрачность корректно передавались вверх по цепочке.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:20 | Создание Drawing слоев (голова, нос, рот, уши, конечности) | Структурирование слоев | Набор Drawing-нод | Критично |
| 2 | 01:21 - 02:10 | Авто-упорядочивание нод ("Order Nodes") | Наведение порядка в Node View | Ровный ряд нод | Желательно |
| 3 | 02:11 - 03:40 | Создание Композитов: `Head_Comp`, `Torso_Comp`, `BodyDown_Comp` | Анатомическое группирование | 3 промежуточных Композита | Критично |
| 4 | 03:41 - 05:00 | Подключение нод к соответственным Композитам | Определение Z-order и вложенности | Иерархические ветви | Критично |
| 5 | 05:01 - 06:10 | Выделение всех Drawing нод и генерация Pegs (`Cmd+Shift+P`) | Добавление трансформационных узлов | Пег над каждой нодой | Критично |

### 4. Визуальные действия в интерфейсе
* **Node View**: Кнопка `Order Nodes` (3 квадрата со стрелкой), создание `Composite` из Node Library, горячие клавиши `Cmd+X`/`Cmd+V`, `Cmd+Shift+P` (Add Peg).

### 5. Повторяемые паттерны
* Массовое создание Drawing-нод под анатомические части.
* Подключение слоев справа налево (Z-depth порядок).
* Прикрепление контроллера (`Peg`) над каждым рисунком.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Создание анатомических слоев | Да | `harmony.drawings.create_layer` | `layer_name`, `node_type` | Имя ноды | Высокая |
| Создание композитов | Да | `harmony.nodes.create` | `node_type="Composite"`, `name` | Имя ноды | Высокая |
| Соединение нод в композиты | Да | `harmony.nodes.connect` | `from_node`, `to_node`, `in_port` | Status True | Высокая |
| Автоматическое добавление пегов | Да | `harmony.rig.create_pegs` | `target_nodes` | Список созданных Pegs | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Добавить в `harmony.rig.create_cutout_hierarchy` параметр авто-генерации под-композитов `create_sub_composites=["Head", "Torso", "Legs"]`.

### 8. Итог по видео
* Видео учит грамотной сборке нодового дерева.
* Ключевые действия: разделение слоев, настройка Z-порядка на Композитах, авто-пеги.

---

## Видео 3: Риггинг в ToonBoom / Node View № 3

### 1. Главная тема видео
Построение связи пегов между собой (`Master Peg`, `Face Peg`, `Eyes Peg`), связывание ног и торса через таз (`Pelvis Peg`), применение `Backdrop` для визуального зонирования Node View и сохранение структуры в библиотеку.

### 2. Что автор пытается донести
Пеги не должны висеть изолированно. Детали лица подчиняются `Face_Peg`, а тот — `Head_Peg`. Это позволяет анимировать поворот/сдвиг лица целиком, а также отдельный зрачок. Использование Backdrops с разными цветами упорядочивает сложные схемы.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:40 | Связывание ушей и лица под `Head_Peg` и `Face_Peg` | Создание иерархии головы | Дерево пегов головы | Критично |
| 2 | 01:41 - 03:00 | Разделение глаз на `Eye_L` и `Eye_R` с отдельными `Comp` | Независимое управление глазами | Подструктура глаз | Критично |
| 3 | 03:01 - 04:20 | Создание `Backdrop` (оранжевый) для головы | Цветовая группировка нод | Визуальный блок | Вспомогательно |
| 4 | 04:21 - 06:00 | Создание `Pelvis_Peg` и разделение ног `Leg_L`/`Leg_R` | Иерархия нижней части тела | Дерево пегов ног | Критично |
| 5 | 06:01 - 07:30 | Оформление Backdrops для Торса и Ног | Чистота схемы | Оформленный Node View | Вспомогательно |
| 6 | 07:31 - 08:30 | Группировка схемы (`Cmd+G`) и сохранение в Library | Создание шаблона для переиспользования | Запись в Library | Желательно |

### 4. Визуальные действия в интерфейсе
* **Node View**: `Insert -> Backdrop`, настройка цвета и имени Backdrop, выделение нод и `Cmd+G` (Group).
* **Library**: Перетаскивание группы нод в раскодированную папку библиотеки.

### 5. Повторяемые паттерны
* Иерархическое подчинение пегов: `Child_Peg -> Parent_Peg -> Master_Peg`.
* Организация цветных подложек `Backdrop`.
* Упаковка дерева нод в `Group`.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Иерархическое связывание пегов | Да | `harmony.nodes.connect` / `harmony.rig.create_cutout_hierarchy` | `hierarchy_map` | Status OK | Высокая |
| Создание группы нод | Да | `harmony.nodes.group` | `node_list`, `group_name` | Group Node Path | Высокая |
| Экспорт шаблона в библиотеку | Да | `harmony.assets.export_template` | `group_node`, `target_path` | Template Path | Высокая |

### 7. Что нужно точечно улучшить в MCP
* В `harmony.nodes.group` добавить возможность создания сопровождающего Backdrop заданного цвета.

### 8. Итог по видео
* Учит анатомической связке пегов и гигиене Node View.

---

## Видео 4: Риггинг в ToonBoom Harmony/ Отрисовываем Морти #4

### 1. Главная тема видео
Послойная отрисовка контуров персонажа с помощью инструментов `Polyline` и `Ellipse`, расстановка шарнирных кругов-ориентиров для пивотов (`Option+B` маркеров) и сквозное (скрытое) дорисовывание суставов.

### 2. Что автор пытается донести
Суставы (плечи, локти, колени) должны иметь идеальную круговую геометрию с нанесенной точкой центра (пивотом). Части тела отрисовываются сквозным методом (не обрезаясь по видимой границе), чтобы при повороте не возникало дыр.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:30 | Настройка инструмента Polyline (толщина 2.0) | Фиксация толщины контура | Единый стиль линий | Критично |
| 2 | 01:31 - 03:00 | Отрисовка эллипсов суставов (плечо, локоть, запястье) | Определение геометрии вращения | Круги суставов | Критично |
| 3 | 03:01 - 04:10 | Простановка маркеров пивота кистью (`Option+B`) | Точная метка центра сустава | Точка в центре круга | Критично |
| 4 | 04:11 - 06:30 | Отрисовка контуров `Polyline` с включенным `Snap` | Замыкание контуров для заливки | Замкнутый контур | Критично |
| 5 | 06:31 - 08:30 | Сквозная отрисовка головы, туловища и ног | Исключение дыр при анимации | Полные геометрические фигуры | Критично |

### 4. Визуальные действия в интерфейсе
* **Tool Properties**: Polyline, Pencil Editor, Snap to Contour (включение магнита для замыкания точек).
* **Hotkeys**: `Option+B` (быстрое переключение на кисть для простановки метки-точки).

### 5. Повторяемые паттерны
* Создание кругов суставов с маркером центра.
* Отрисовка сегментов `Polyline` с обязательным смыканием точек (`Snap`).
* Сквозное перекрытие геометрических объемов.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Отрисовка геометрии суставов | Частично | `harmony.drawings.create_drawing` | `drawing_data`, `layer_name` | Drawing ID | Средняя (требует векторных данных) |
| Авто-проверка замкнутости линий | Да | `harmony.audit.find_empty_drawings` / custom rule | `layer_name` | Analysis result | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Добавить функцию автоматической проверки замыкания векторных контуров (Gap detection).

### 8. Итог по видео
* Учит правилам векторной отрисовки под Cut-out (круги суставов, сквозные формы).

---

## Видео 5: Rig in ToonBoom / Pretty Morty #5

### 1. Главная тема видео
Раскрашивание персонажа (Paint Tool), удаление временных суставных грибков, выстраивание рукавов как отдельных слоев и расстановка пивотов (`Rotate Tool` на пегах).

### 2. Что автор пытается донести
Пивоты должны устанавливаться строго на `Peg`-нодах, а не на рисунках (`Drawing`). Пивот сустава совмещается точно с маркерной точкой, нарисованной в Уроке #4. Вынесение рукавов на отдельные слои предотвращает слипание контура руки и футболки.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 02:00 | Коррекция суставов ног и удаление временных дуг | Подготовка контура ноги | Чистый вектор ноги | Желательно |
| 2 | 02:01 - 03:50 | Выделение рукавов на отдельные слои и пеги | Естественное перекрытие руки | Слои `Sleeve_L`/`Sleeve_R` | Критично |
| 3 | 03:51 - 06:00 | Заливка цвета `Paint Tool` по слоям палитры | Закрашивание персонажа | Цветной персонаж | Критично |
| 4 | 06:01 - 08:29 | Точная расстановка Pivot Points на Pegs | Фиксация точек вращения | Выставленные пивоты | Критично |

### 4. Визуальные действия в интерфейсе
* **Advanced Animation Tools**: `Rotate Tool` (для перемещения центра вращения Peg).
* **Color View**: Заливка `Paint Tool` (выбор цвета из палитры).

### 5. Повторяемые паттерны
* Перемещение пивота на Peg с привязкой к нарисованному центру сустава.
* Проверка вращения конечности сразу после установки пивота.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Установка пивота пега | Да | `harmony.nodes.set_attr` / `harmony.rig.create_pegs` | `node`, `attr="pivot"`, `x`, `y` | Status OK | Высокая |
| Заливка цветом по именованной палитре | Да | `harmony.drawings.replace_drawing` | `layer_name`, `color_name` | Success | Высокая |

### 7. Что нужно точечно улучшить в MCP
* В `harmony.nodes.set_attr` добавить валидатор координаты пивота (проверка на NaN/Null).

### 8. Итог по видео
* Учит точной выставке пивотов и послойному закрашиванию.

---

## Видео 6: Риг Toon Boom Harmony простой урок // #6a РУКИ

### 1. Главная тема видео
Продвинутый риггинг руки (Способ 1): разделение заливки и контура по суб-слоям (`Line Art`, `Color Art`), применение нод `Layer Selector`, `Auto Patch` и `Cutter` для бесшовного соединения локтя и запястья.

### 2. Что автор пытается донести
Для создания единой «живой» руки без видимых швов контур выносится на `Line Art`, а заливка — на `Color Art`. Нода `Auto Patch` автоматически подрезает контур стыка, оставляя только внешнюю заливку. `Cutter` используется для маскирования запястья под кисть.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:40 | Добавление `Layer Selector` (3 шт), `Auto Patch`, `Cutter` | Подготовка узлов фильтрации | Набор маскирующих нод | Критично |
| 2 | 01:41 - 03:00 | Разделение рисунка плеча/предплечья на `Line Art` и `Color Art` | Изоляция контура от заливки | Разнесенные суб-слои | Критично |
| 3 | 03:01 - 04:30 | Настройка `Layer Selector` (`Line Art` / `Color Art`) | Фильтрация потока данных | Настроенные селекторы | Критично |
| 4 | 04:31 - 05:30 | Подключение `Auto Patch` к нижнему сегменту руки | Перекрытие контура сустава заливкой | Бесшовный локоть | Критично |
| 5 | 05:31 - 06:30 | Подключение `Cutter` под маску кисти | Обрезка края предплечья | Чистый стык кисти | Критично |

### 4. Визуальные действия в интерфейсе
* **Sub-layers View**: Переключение и вырезание содержимого между `Line Art` (`L`) и `Color Art` (`C`).
* **Node View**: Подключение `Auto Patch` и `Cutter` (инвертирование маски двойным кликом).

### 5. Повторяемые паттерны
* Вынос контура на `Line Art`, заливки — на `Color Art`.
* Подключение `Auto Patch` к источнику заливки для перекрытия нижнего контура.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Автоматическое создание цепочки AutoPatch + LayerSelector | Да | `harmony.nodes.create_effect_chain` / `harmony.rig.create_cutout_hierarchy` | `type="autopatch_arm"`, `nodes` | Created chain structure | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Создать готовый хелпер-пресет в `harmony.nodes.create_effect_chain` под названием `arm_seamless_autopatch`.

### 8. Итог по видео
* Учит профессиональному способу бесшовных рук через AutoPatch + Cutter.

---

## Видео 7: Риггинг в Toon Boom Harmony уроки / #6b РУКИ

### 1. Главная тема видео
Альтернативный риггинг руки (Способ 2 - упрощенный): использование `Auto Patch` и линии на суб-слое `Overlay` без ноды `Cutter` для формирования складки при сгибе.

### 2. Что автор пытается донести
Второй способ проще первого: не требуются ноды `Cutter`. Заливка смещается на `Color Art`, а для отображения линии изгиба при поднятии руки рисуется отдельный отрезок на суб-слое `Overlay`. Это экономит ноды и упрощает сцену.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:20 | Изменение Z-порядка: средняя часть руки под верхнюю | Базовое перекрытие | Измененный порядок нод | Критично |
| 2 | 01:21 - 02:30 | Подключение `Auto Patch` и `Overlay` к верхней части руки | Перекрытие шва и вынос линии складки | Сетка `AutoPatch` + `Overlay` | Критично |
| 3 | 02:31 - 03:50 | Копирование линии складки на суб-слой `Overlay` | Рисование линии изгиба локтя | Динамическая складка | Критично |
| 4 | 03:51 - 05:00 | Коррекция рукава под слой `Overlay` | Завершение сборки 2-го способа | Готовая легкая рука | Критично |

### 4. Визуальные действия в интерфейсе
* **Sub-layers View**: Работа с суб-слоем `Overlay` (`O`).
* **Tool Properties**: Pencil Editor для скругления кончиков линии складки.

### 5. Повторяемые паттерны
* Вынос штриха изгиба на `Overlay`.
* Использование `Auto Patch` без внешних масок.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Сборка упрощенного AutoPatch | Да | `harmony.nodes.create_effect_chain` | `type="simple_overlay_arm"` | Chains result | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Добавить поддержку параметра `use_overlay_fold=true` в билдерах рук.

### 8. Итог по видео
* Учит более легкому методу создания рук с линией складки.

---

## Видео 8: Сохраняем персонажа в библиотеку Toon Boom

### 1. Главная тема видео
Сохранение персонажа в глобальную или проектную библиотеку (Library) Toon Boom Harmony, разблокировка прав записи (`Right-click -> Write to Modify`) и перенос через `Cmd+C` / `Cmd+V`.

### 2. Что автор пытается донести
Чтобы использовать риг в других сценах, папка библиотеки должна быть открыта на запись (`Right-click -> Right to Modify`). Прямое перетаскивание группы мышью из Node View может блокироваться системой, поэтому надежный способ — скопировать ноду-группу (`Cmd+C`) и вставить в окно Library (`Cmd+V`).

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:30 | Открытие внешней папки через `Open Library` | Подключение каталога на диске | Отображаемая папка | Критично |
| 2 | 01:31 - 02:40 | Снятие блокировки `Right to Modify` | Получение прав на запись | Зеленая открытая папка | Критично |
| 3 | 02:41 - 04:30 | Копирование группы персонажа (`Cmd+C`) и вставка в Library | Сохранение `.tpl` шаблона | Шаблон персонажа `.tpl` | Критично |
| 4 | 04:31 - 05:13 | Тестирование импорта шаблона в пустую сцену | Проверка работоспособности | Восстановленный риг | Желательно |

### 4. Визуальные действия в интерфейсе
* **Library Panel**: Контекстное меню папки -> `Right to Modify`.
* **Shortcuts**: `Cmd+C` на ноде в Node View, `Cmd+V` в панели Library.

### 5. Повторяемые паттерны
* Разблокировка доступа к папке шаблонов.
* Сохранение элементов персонажа в форме `.tpl`.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Экспорт персонажа в шаблон | Да | `harmony.assets.export_template` | `node_path`, `template_path` | Success status | Высокая |
| Импорт шаблона в сцену | Да | `harmony.assets.import_template` | `template_path`, `target_composite` | Imported Node ID | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Добавить авто-проверку прав доступа к целевой директории перед экспортом шаблона.

### 8. Итог по видео
* Учит экспорту/импорту шаблонов персонажей.

---

## Видео 9: Rig in Toon Boom Harmony / Legs and Pelvis #7

### 1. Главная тема видео
Риггинг ног и таза: сборка ног через `Auto Patch` и `Overlay`, создание отдельного композита для таза (`Pelvis_Comp`) и устранение разрывов джинс при сгибе ног.

### 2. Что автор пытается донести
Таз объединяет обе ноги. Чтобы джинсы при сгибе ноги не образовывали дыр, стык верхнего основания ноги и таза закрывается нодами `Auto Patch` и выносится в единый `Pelvis_Comp`.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:40 | Иерархическое перераспределение: ботинок -> голень -> бедро | Фиксация Z-depth | Измененная иерархия | Критично |
| 2 | 01:41 - 03:00 | Разделение рисунка ног на `Line Art` и `Color Art` | Подготовка под AutoPatch | Подготовленные суб-слои | Критично |
| 3 | 03:01 - 04:30 | Создание `Pelvis_Comp` и подключение `Auto Patch` ног | Бесшовное примыкание к тазу | Соединенный таз и ноги | Критично |
| 4 | 04:31 - 06:00 | Добавление ноды `Overlay` для сохранения линии шагового шва | Динамический шов брюк | Готовый риг ног | Критично |

### 4. Визуальные действия в интерфейсе
* **Node View**: Создание `Pelvis_Comp`, привязка нод `Auto Patch` обеих ног.

### 5. Повторяемые паттерны
* Бесшовное связывание парных конечностей с центральным узлом (тазом).

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Авто-сборка иерархии ног и таза | Да | `harmony.rig.create_cutout_hierarchy` | `limb_type="legs"`, `pelvis=true` | Node tree structure | Высокая |

### 7. Что нужно точечно улучшить в MCP
* В рецептах MCP добавить проверку наличия шагового шва брюк при сборке гуманоидных ног.

### 8. Итог по видео
* Учит созданию бесшовного узла «Таз-Ноги».

---

## Видео 10: Риггинг в Toon Boom Harmony (урок) // #8 ГЛАЗА

### 1. Главная тема видео
Риггинг лица: создание бровей, обрезка зрачков внутри белка глаза нодой `Cutter` (маска) и создание уникального эмоушн-деформера брови с помощью слоя `Overlay` и инструмента `Stroke` (горячая клавиша `K`).

### 2. Что автор пытается донести
Зрачок должен быть ограничен белком глаза — для этого применяется `Cutter`. Чтобы бровь могла перекрывать верхний край глаза и образовывать любые эмоции, создается деформер `Curve Deformer`, а на суб-слой `Overlay` наносится патч-заливка цветной маской через видимые направляющие (`K`).

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 01:30 | Отрисовка бровей на отдельном слое `Brows` | Подготовка геометрии бровей | Векторные брови | Критично |
| 2 | 01:31 - 03:20 | Сборка схемы глаза: `Sclera` -> `Cutter` <- `Pupil` | Ограничение вылета зрачка | Замаскированный зрачок | Критично |
| 3 | 03:21 - 04:50 | Настройка инверсии `Cutter` (двойной клик на ноде) | Зрачок виден только на белке | Корректный глаз | Критично |
| 4 | 04:51 - 06:40 | Добавление `Curve Deformer` на бровь | Создание возможности изгиба | Деформер брови | Критично |
| 5 | 06:41 - 08:31 | Рисование невидимки `Stroke` (`K`), заливка маски на `Overlay` | Перекрытие глаза бровью при мимике | Динамическая бровь | Желательно |

### 4. Визуальные действия в интерфейсе
* **Node View**: `Cutter` (Double Click для инверсии маски), `Layer Selector`.
* **Camera View**: `Rigging Tool -> Create New Deformation`, просмотр обводок кнопкой `K` (`Show/Hide Strokes`).

### 5. Повторяемые паттерны
* Маскирование зрачка белком глаза (`Cutter`).
* Оснащение брей деформером с перекрывающей маской на `Overlay`.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Создание глазной системы с маской | Да | `harmony.rig.create_eye_system` | `eye_name`, `pupil_node`, `sclera_node` | Node system status | Высокая |
| Добавление деформера брови | Да | `harmony.rig.create_brow_system` | `brow_layer`, `deformer_type="curve"` | Created deformer node | Высокая |

### 7. Что нужно точечно улучшить в MCP
* Добавить авто-инвертирование порта `Cutter` в `harmony.rig.create_eye_system`.

### 8. Итог по видео
* Учит маскированию глаз и эмоциональным деформерам бровей.

---

## Видео 11: Простой Риг в Toon Boom Harmony (урок) // #9 Деформеры / 12+

### 1. Главная тема видео
Обзор и установка деформеров: `Bone Deformer` (для твердых конечностей и тела), `Curve Deformer` (для изгибающихся рук/волос), `Envelope Deformer` (для формы одежды/живота). Подключение ноды `Kinematic Output` для изоляции трансформаций.

### 2. Что автор пытается донести
`Bone` используется на твердых костях, `Curve` — на шланговых руках и бровях, `Envelope` — на замкнутых контурных формах. Если привязать руку к деформируемому телу напрямую, руку искривит вместе с телом. Чтобы этого не произошло, используется нода `Kinematic Output`.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 02:10 | Разбор 3 типов: Bone, Curve, Envelope | Выбор инструмента под задачу | Понимание типов | Вспомогательно |
| 2 | 02:11 - 03:40 | Установка `Curve Deformer` на руки и ноги | Придание гибкости конечностям | Деформеры рук/ног | Критично |
| 3 | 03:41 - 04:50 | Установка `Bone Deformer` на тело (от таза к голове) | Позвоночник персонажа | Костный деформер тела | Критично |
| 4 | 04:51 - 06:30 | Вставка нод `Kinematic Output` перед пегами рук и ног | Изоляция искажений тела от конечностей | Чистые связи пегов | Критично |
| 5 | 06:31 - 07:54 | Проверка движения рига в позе | Финальный тест сочленений | Рабочий Cut-out риг | Критично |

### 4. Визуальные действия в интерфейсе
* **Rigging Tool Panel**: Выбор режимов `Bone`, `Curve`, `Envelope`.
* **Node View**: Добавление `Kinematic Output` между группой деформера и подчиненным пегом.

### 5. Повторяемые паттерны
* Прокладка цепи деформера по анатомическим точкам.
* Обязательное изолирование ветвей через `Kinematic Output`.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Генерация деформеров | Да | `harmony.rig.create_deformers` | `target_nodes`, `deformer_type` | Deformer chain IDs | Высокая |
| Вставка Kinematic Output | Да | `harmony.nodes.create` + `harmony.nodes.connect` | `node_type="KinematicOutput"` | Status OK | Высокая |
| Проверка валидности деформеров | Да | `harmony.rig.validate_deformers` | `character_node` | Validation report | Высокая |

### 7. Что нужно точечно улучшить в MCP
* В `harmony.rig.create_deformers` автоматически добавлять `Kinematic Output` для всех дочерних веток.

### 8. Итог по видео
* Учит правильному деформированию тела без искажения дочерних элементов.

---

## Видео 12: Риг 360 (поворотный) / урок риггинг в Toon Boom Harmony

### 1. Главная тема видео
Создание поворотного рига (ракурс 3/4): отрисовка новых фаз на существующих слоях, изменение Z-глубины слоев (`Option + Up/Down`), добавление альтернативных деформеров через `Create New Deformation`.

### 2. Что автор пытается донести
Для поворота на 360 градусов не создаются новые слои — новые рисунки добавляются в те же Drawing-ноды под новыми номерами (Substitutions). На каждый ракурс настраивается свой собственный деформер через функцию `Create New Deformation`. Смещение по Z-оси регулирует перекрытие рук относительно тела.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 02:00 | Импорт референса 3/4 и подгонка масштаба | Ориентир для отрисовки | Масштабированный арт | Желательно |
| 2 | 02:01 - 04:30 | Отрисовка рисунков 3/4 на существующих слоях | Создание подстановок (Substitutions)| Ракурс 3/4 | Критично |
| 3 | 04:31 - 05:40 | Смещение Z-depth слоев (`Option + Up/Down`) | Завод руки/уха за туловище | Корректное перекрытие | Критично |
| 4 | 05:41 - 07:30 | Создание нового деформера через `Create New Deformation` | Изоляция деформации для ракурса 3/4 | Второй деформер в цепи | Критично |
| 5 | 07:31 - 08:01 | Тестирование поворота и переключения фаз | Валидация позы 3/4 | Поворотный персонаж | Критично |

### 4. Визуальные действия в интерфейсе
* **Tool Properties**: `Create New Deformation` (плюс на панели деформера).
* **Hotkeys**: `Option + Up / Down` (или `Alt + Up / Down`) для тонкого сдвига по Z-оси.

### 5. Повторяемые паттерны
* Добавление фаз в существующие ноды рисунков.
* Создание независимых деформеров под каждый ракурс.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Создание 360 структуры | Да | `harmony.rig360.create_body_360_structure` | `character_name`, `angles` | 360 Rig Structure | Высокая |
| Маппинг подстановок по углам | Да | `harmony.rig360.map_drawings_to_angles` | `angle="3_4"`, `drawing_map` | Mapping status | Высокая |

### 7. Что нужно точечно улучшить в MCP
* В `harmony.rig360.map_drawings_to_angles` добавить авто-создание уникальных деформационных групп под каждый угол.

### 8. Итог по видео
* Учит принципам построения поворотного 360-рига.

---

## Видео 13: Простой риггинг / библиотека Toon Boom Harmony

### 1. Главная тема видео
Работа с библиотекой (Library): кастомизация панелей, удаление и сортировка элементов, организация подстановок (Drawing Substitutions `Shift+Option+D`), продление экспозиции (`F5`) и сохранение циклов анимации/мимики в шаблоны `.tpl`.

### 2. Что автор пытается донести
Библиотека служит не только для хранения ригов, но и для переиспользования готовых анимационных циклов (подмигивание, походка, артикуляция). Сохранение коротких `.tpl` файлов с ключами позволяет собирать анимацию из готовых пресетов.

### 3. Пошаговая последовательность действий автора

| Шаг | Таймкод | Что делает автор | Зачем он это делает | Что должно получиться | Важность |
|---|---|---|---|---|---|
| 1 | 00:00 - 02:00 | Настройка видов Library (Thumbnails, List, Details) | Удобство навигации | Настроенный вид | Вспомогательно |
| 2 | 02:01 - 03:40 | Создание новых рисунков через `Shift+Option+D` | Формирование подстановок глазу | Набор фаз глаза | Критично |
| 3 | 03:41 - 04:50 | Продление экспозиции кадров клавишей `F5` | Удержание рисунка во времени | Заполненный таймлайн | Критично |
| 4 | 04:51 - 06:30 | Перетаскивание выделенных кадров таймлайна в Library | Создание клипа анимации `.tpl` | Пресет анимации | Желательно |
| 5 | 06:31 - 06:53 | Повторное использование пресета в другой позиции | Ускорение анимации | Вставленный клип | Желательно |

### 4. Визуальные действия в интерфейсе
* **Timeline View**: `F5` (Extend Exposure), `Shift+Option+D` (Duplicate Drawing / New Version).
* **Library View**: Выделение фрагмента таймлайна -> D&D в открытую папку.

### 5. Повторяемые паттерны
* Создание библиотеки подстановок (Substitutions).
* Сохранение клипов экспозиции в `.tpl`.

### 6. Связь с существующим MCP-сервером

| Действие автора | Можно ли выполнить через MCP? | Какой тип существующего tool нужен | Что tool должен получить на вход | Что tool должен вернуть | Уверенность |
|---|---|---|---|---|---|
| Управление подстановками рисунков | Да | `harmony.drawings.list_substitutions` / `harmony.drawings.replace_drawing` | `layer_name`, `drawing_id` | Status OK | Высокая |
| Настройка экспозиции кадров | Да | `harmony.timeline.set_exposure` | `layer_name`, `start_frame`, `end_frame` | Exposure OK | Высокая |

### 7. Что нужно точечно улучшить в MCP
* В `harmony.timeline.set_exposure` добавить команду создания новой дублированной фазы (`duplicate_drawing=true`).

### 8. Итог по видео
* Учит работе с подстановками рисунков и сохранению анимационных пресетов.

---

# ЧАСТЬ 2: Единая База Знаний (`playlist_knowledge_base.md`)

---

## 1. Общая логика курса
Курс построен от простого к сложному по строго определенной методической цепочке:
1. **Фундамент (Урок 1)**: Глобальная настройка сцены (изоляция осей, разнос слоев, палитра).
2. **Архитектура (Уроки 2–3)**: Сборка нодового каркаса (Композиты, Пеги, Иерархические связи).
3. **Геометрия и Арт (Уроки 4–5)**: Векторная отрисовка со сквозными суставами и расстановка пивотов.
4. **Специализированные сочленения (Уроки 6–10)**:
   * Руки (AutoPatch, LayerSelector, Cutter, Overlay).
   * Ноги и Таз (Pelvis Composite, бесшовные джинсы).
   * Лицо и Глаза (Cutter для зрачков, эмоушн-маски бровей).
5. **Динамика (Урок 11)**: Деформеры (Bone, Curve, Envelope) и изоляция через Kinematic Output.
6. **Масштабирование (Уроки 12–13)**: Поворот 360 (ракурс 3/4), библиотека подстановок и экспорт `.tpl`.

---

## 2. Полная последовательность создания рига

1. **Конфигурация среды**:
   * Включить `Default Separate Position`.
   * Включить `Support Overlay and Underlay`.
   * Выключить `Element Node Creation`.
2. **Подготовка цвета**:
   * Импортировать PNG с конвертацией в вектор (`Color Mode`).
   * Создать персонажную палитру и называть каждый цвет по имени узла.
3. **Нодовый каркас**:
   * Создать Drawing-ноды для всех анатомических элементов.
   * Создать композиты `Head_Comp`, `Torso_Comp`, `Pelvis_Comp`, `Master_Comp`.
   * Сгенерировать Пеги (`Cmd+Shift+P`) над всеми нодами.
4. **Иерархическая связка**:
   * Связать пеги в дерево: `Master_Peg -> Body_Peg -> Pelvis/Torso/Head_Pegs`.
   * Оформить визуальные подложки `Backdrop`.
5. **Векторный арт и шарниры**:
   * Нарисовать круги суставов с точкой центра (`Option+B`).
   * Отрисовать сквозные объемные контуры (`Polyline` + `Snap`).
   * Залить цвета инструментом `Paint Tool`.
6. **Выстановка пивотов**:
   * Выставить центральные колышки (`Rotate Tool`) на Pegs строго по маркерным точкам.
7. **Оснащение бесшовных сочленений**:
   * Собрать цепочки `Auto Patch` + `Layer Selector` для рук и ног.
   * Вынести складки на суб-слой `Overlay`.
   * Замаскировать зрачки в белках глаз нодой `Cutter`.
8. **Прокладка деформеров**:
   * Повесить `Curve Deformer` на рукава, руки и ноги.
   * Повесить `Bone Deformer` на позвоночник.
   * Вставить ноды `Kinematic Output` между деформерами и дочерними пегами.
9. **Поворот и Библиотека**:
   * Добавить фазу 3/4 в подстановки (`Substitutions`).
   * Создать доп. цепи деформаций (`Create New Deformation`).
   * Заковать риг в `Group` и экспортировать `.tpl` в Library.

---

## 3. Карта паттернов

| Паттерн | Где встречается | Для чего нужен | Какие действия включает | Какие MCP tools нужны |
|---|---|---|---|---|
| **Pivot Matching** | Все суставы (плечи, локти, колени) | Точное вращение без излома | Рисование круга с точкой, совмещение Pivot Peg с точкой | `harmony.rig.create_pegs`, `harmony.nodes.set_attr` |
| **Seamless Joint (AutoPatch)** | Руки, Ноги, Таз | Скрытие контура сустава | Смещение заливки на `Color Art`, фильтрация `Auto Patch` | `harmony.nodes.create_effect_chain` |
| **Eye Cutter Mask** | Глаза | Удержание зрачка внутри белка | `Cutter (Inverted)` между белком и зрачком | `harmony.rig.create_eye_system` |
| **Kinematic Isolation** | Ветви деформеров | Предотвращение искажения дочерних элементов | Вставка ноды `Kinematic Output` перед пегом руки/ноги | `harmony.rig.create_deformers` |
| **Multi-Angle Deformation** | Поворот 360 | Разные формы деформации для ракурсов | `Create New Deformation` под новый `Drawing` | `harmony.rig360.map_drawings_to_angles` |

---

## 4. Карта “ручное действие → MCP-действие”

| Ручное действие в Toon Boom | Как это объясняет автор | Как это можно выразить для MCP | Какие параметры нужны |
|---|---|---|---|
| Выключение `Element Node Creation` | "Чтобы ключи анимации не появлялись на рисунках" | `harmony.validate_environment` / `harmony.get_config` | `element_node_creation: false` |
| Создание пегов `Cmd+Shift+P` | "Выделяем все слои и нажимаем горячую клавишу" | `harmony.rig.create_pegs` | `target_nodes: [...]` |
| Сборка AutoPatch руки | "Подключаем Layer Selector на Color Art и вставляем AutoPatch" | `harmony.nodes.create_effect_chain` | `chain_type: "autopatch"`, `target_node` |
| Выставка пивота на сустав | "Берем Rotate Tool и ставим колышек на черную точку" | `harmony.nodes.set_attr` | `node`, `pivot_x`, `pivot_y` |
| Маскирование зрачка | "Два раза щелкаем на Cutter, чтобы инвертировать" | `harmony.rig.create_eye_system` | `inverted_mask: true` |

---

## 5. Рецепты использования существующего MCP-сервера

### Recipe 1: Создание базовой иерархии персонажа
* **Когда использовать**: В самом начале работы над новым персонажем.
* **Последовательность MCP вызовов**:
  1. `harmony.drawings.create_layer` (для всех элементов: `Head`, `Torso`, `Arm_L`, `Arm_R`, `Leg_L`, `Leg_R`).
  2. `harmony.nodes.create` (типы `Composite`: `Head_Comp`, `Torso_Comp`, `Pelvis_Comp`, `Master_Comp`).
  3. `harmony.nodes.connect` (сортировка справа налево по Z-depth).
  4. `harmony.rig.create_pegs` (создание управляющих пегов).
  5. `harmony.nodes.connect` (связывание пегов в анатомическое дерево).
* **Проверка**: Вызов `harmony.audit.find_broken_connections`.

### Recipe 2: Оснащение бесшовной руки (Seamless Arm)
* **Когда использовать**: На этапе настройки сочленений плеча и локтя.
* **Последовательность MCP вызовов**:
  1. `harmony.nodes.create_effect_chain` (параметр `chain_type="arm_autopatch"`).
  2. `harmony.nodes.connect` (подключение `Line Art` к композиту, `Color Art` к `Auto Patch`).
  3. `harmony.rig.create_deformers` (создание `Curve Deformer` по вехам плеча и локтя).
* **Проверка**: `harmony.rig.validate_deformers`.

### Recipe 3: Оснащение глаза с маской зрачка
* **Когда использовать**: При риггинге лица.
* **Последовательность MCP вызовов**:
  1. `harmony.rig.create_eye_system` (передача нод `Sclera`, `Pupil`).
  2. `harmony.nodes.set_attr` (установка `inverted=true` на ноде `Cutter`).
* **Проверка**: Тестовый сдвиг зрачка за пределы белка.

### Recipe 4: Проверка и валидация рига
* **Когда использовать**: Перед передачей персонажа в анимацию.
* **Последовательность MCP вызовов**:
  1. `harmony.audit.scene` (поиск незамкнутых связей).
  2. `harmony.rig.validate_naming` (проверка соответствия именования).
  3. `harmony.rig.validate_deformers` (проверка наличия `Kinematic Output`).

---

## 6. Что сервер уже должен уметь
На основе уроков MCP-сервер должен гарантировать выполнение следующих операций:
1. Полное авто-построение Cut-out дерева нод со всеми промежуточными композитами.
2. Автоматическое формирование связей `Auto Patch` + `Layer Selector` + `Cutter`.
3. Генерация деформационных цепей (`Bone`, `Curve`, `Envelope`) с обязательной вставкой `Kinematic Output`.
4. Оснащение глазных систем инвертированными масками.
5. Экспорт и импорт шаблонов `.tpl` с сохранением иерархии и палитры.

---

## 7. Что нужно доработать точечно

| Инструмент MCP | Что добавить | Зачем | На каком уроке основано | Важность |
|---|---|---|---|---|
| `harmony.validate_environment` | Добавить проверку `element_node_creation` и `separate_position` | Исключение ошибок анимации на слоях рисунков | Урок 1 | Критично |
| `harmony.nodes.create_effect_chain` | Добавить шаблон `seamless_limb` | Авто-генерация узла AutoPatch + LayerSelector | Уроки 6, 7, 9 | Критично |
| `harmony.rig.create_deformers` | Авто-вставка `Kinematic Output` | Изоляция искажений деформера от дочерних элементов | Урок 11 | Критично |
| `harmony.rig.create_eye_system` | Авто-настройка инверсии `Cutter` | Избавление от ручной настройки маски зрачка | Урок 10 | Желательно |
| `harmony.rig360.map_drawings_to_angles` | Добавить привязку `Create New Deformation` | Корректный поворот 360 с уникальными деформерами | Урок 12 | Желательно |

---

## 8. Что нельзя автоматизировать полностью
Следующие художественные и творческие этапы требуют обязательного участия человека:
1. **Первичный эстетический рисунок**: Прорисовка характера персонажа и его пропорций.
2. **Художественный выбор толщины линии**: Ручная подгонка толщины и формы кончиков штриха (`Pencil Editor`).
3. **Тонкая пластика мимики**: Визуальная оценка эстетичности эмоций при изгибе бровей и рта.
4. **Дизайн формы костюма**: Определение мест складок одежды при сгибах.

---

## 9. Словарь терминов автора

* **Node View (Нодовая система)**: Окно визуального графа связей элементов сцены.
* **Peg (Пег)**: Управляющий узел трансформации (позиция, поворот, масштаб), находящийся над рисунком.
* **Composite (Композит)**: Узел объединения графических потоков и управления Z-порядком.
* **Pass Through**: Режим композита, пропускающий Z-информацию каждого слоя отдельно.
* **Auto Patch**: Нода автоматической обрезки контура для создания бесшовных суставов.
* **Layer Selector**: Нода-фильтр, пропускающая только выбранный суб-слой (`Line Art`, `Color Art`, `Overlay`, `Underlay`).
* **Cutter (Маска)**: Нода обрезки одного слоя другим (например, зрачка белком).
* **Kinematic Output**: Нода-изолятор, передающая только трансформации пега, но отсекающая деформации `Deformer`.
* **Substitutions (Подстановки)**: Набор альтернативных рисунков внутри одного слоя (например, разные формы рта или глаза).
* **Backdrop (Подложка)**: Цветной прямоугольный блок в Node View для визуальной группировки нод.

---

## 10. Финальный вывод
Учебный курс Гликерии Узинской демонстрирует четкую, высокостандартизированную инженерно-анимационную систему. Главная ценность курса заключается не просто в рисовании персонажа, а в **создании предсказуемой архитектуры**, где:
1. Каждая деталь имеет собственный Peg.
2. Каждое сочленение изолировано от разрывов через AutoPatch или Overlay.
3. Каждая деформация защищена от искажения сопредельных веток нодой Kinematic Output.

Интеграция этих знаний в MCP-сервер `toonboom-harmony-mcp` позволяет превратить рутинную работу по сборке нод, подрезке масок и связыванию пегов в автоматические высокоуровневые вызовы, оставляя человеку исключительно творческую составляющую — дизайн, пластику и финальную анимацию.
